import React from "react";
import styles from "./styles.module.scss";

const Examples: React.FC = () => {
    return (
        <div className={styles.outerContainer}>
            <h2 className={styles.mainText}>Examples coming soon!</h2>
        </div>
    );
};

export default Examples;
